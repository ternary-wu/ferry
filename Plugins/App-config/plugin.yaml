name: App Config
version: 1.0.0
target:
  name: application
  version: ">=1.0"
  note: 通用示例插件，字段随目标应用扩展
author: Ferry Community
description: 生成应用配置文件（YAML 格式，支持导入）
renderer:
  type: yaml
  defaultFileName: app-config.yaml
  outputExtension: .yaml
